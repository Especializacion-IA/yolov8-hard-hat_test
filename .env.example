# Roboflow API Key for dataset access
ROBOFLOW_API_KEY=your_roboflow_api_key_here

# Model Configuration
MODEL_SIZE=n  # Options: n, s, m, l, x (nano, small, medium, large, xlarge)
CONFIDENCE_THRESHOLD=0.25  # Detection confidence threshold
